plugins {
    id 'java'
    id 'application'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':net')
    // Use JUnit test framework.
    testImplementation 'junit:junit:4.13.1'
}

test {
    useJUnitPlatform()
}